import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  changeTimeButton: {
    padding: 5,
    height: "50%",
    alignItems: "center",
    justifyContent: "center",
  },
});
